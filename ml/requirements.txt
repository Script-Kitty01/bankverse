# BankVerse ML training dependencies — pinned for reproducible pipeline runs.
# Scope: Phase 4 training only. Serving dependencies (FastAPI/Uvicorn) are kept
# separate so the training environment stays minimal.
pandas==3.0.5
numpy==2.5.2
scipy==1.18.1
scikit-learn==1.9.0
xgboost==3.4.1
shap==0.52.0
joblib==1.6.0
